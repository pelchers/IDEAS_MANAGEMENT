# frontend-design-subagent

Generates one isolated style/pass frontend concept in `.docs/planning/concepts/<style>/pass-<n>` with external inspiration cross-reference and writes validation outputs to that pass `validation/` subfolder.
