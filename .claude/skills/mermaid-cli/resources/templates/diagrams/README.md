# Diagram Templates

These starter files cover the current Mermaid diagram families documented on `mermaid.js.org`.

Render any template with:

```bash
mmdc -i path/to/template.mmd -o output.svg
```

Validate the full set with:

```bash
powershell -ExecutionPolicy Bypass -File resources/scripts/validate-template-suite.ps1
```
