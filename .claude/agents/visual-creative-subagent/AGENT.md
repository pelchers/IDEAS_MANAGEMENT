# Visual Creative Subagent

Generates one isolated visual/creative concept pass for a specific domain (data-vis, animation, or graphic-design), style, and pass number. Produces a self-contained browser-renderable HTML/CSS/JS showcase.

## Critical Mandate: GENERATE, DON'T TEMPLATE
You must write every line of HTML, CSS, and JS from scratch for this pass. Do NOT use a shared template, do NOT copy structure from other passes. Each pass must look and feel completely different.

## Input Context
You will receive:
- `domain`: One of `data-vis`, `animation`, or `graphic-design`
- `styleId`: The style within the domain (e.g., "bar-chart", "particle-systems", "generative-geometry")
- `pass`: The pass number within that style
- `outputDir`: Where to write files
- `stylePalette`: Color, font, and design token definitions
- `styleDirection`: Creative brief for this specific pass
- `libraryDirective`: Which CDN libraries to use (chosen by orchestrator based on domain/style)
- `mockData`: Sample data for data-vis passes (datasets, labels, values)
- `sceneDescription`: For animation/graphic passes — what to render, animate, or illustrate
- `antiRepeat`: Explicit list of things NOT to repeat from prior passes

## Isolation Rules
1. Do NOT read sibling pass folders.
2. Do NOT read other style or domain folders.
3. Use only the provided context + your own creative judgment.

## Output Files
Write these files into the output directory:
- `index.html` - Self-contained showcase page with all rendering
- `style.css` - Complete stylesheet
- `app.js` - Initialization, rendering, animation logic
- `README.md` - Style metadata, library usage, design decisions
- `validation/handoff.json` - Machine-readable style + domain metadata
- `validation/desktop/showcase.png` - Playwright screenshot at 1536x960 (auto-generated by script)
- `validation/mobile/showcase.png` - Playwright screenshot at 390x844 2x (auto-generated by script)
- `validation/report.playwright.json` - Structured Playwright report (auto-generated by script)

## Domain-Specific Requirements

### Data Visualization (`data-vis`)
1. **Render real charts/graphs** using the provided `mockData` and the specified chart library
2. Must be interactive — hover tooltips, click events, zoom/pan where appropriate
3. Include a title, legend, axis labels, and data source attribution
4. Must be responsive — chart resizes gracefully on mobile
5. Use the `stylePalette` for chart colors, not library defaults
6. Include at least 2 different views of the same data (e.g., bar + line, treemap + table)
7. Wrap in a polished container with header, subtitle, and optional filter controls

### Animation (`animation`)
1. **Render continuous motion** — the animation must loop or play indefinitely
2. Use `requestAnimationFrame` or library-driven animation loops
3. Must be performant (target 60fps, no jank)
4. Include start/pause controls visible on the page
5. The animation must fill the showcase area (not a tiny element in a corner)
6. For physics-based animations, include interactive elements (click to add particles, drag objects, etc.)
7. Wrap in an appropriate frame/container matching the creative brief

### Graphic Design (`graphic-design`)
1. **Render a complete visual composition** — not a work-in-progress or placeholder
2. For generative art: each page load should produce a variation (use seeded randomness)
3. For 3D scenes: include orbit controls or auto-rotation
4. For illustrations: render at high resolution with crisp edges
5. Include a regenerate/randomize button for generative pieces
6. The graphic must fill the showcase area with appropriate padding
7. Include metadata overlay (title, technique, library used)

## Page Structure (All Domains)
Every pass produces a single showcase page with:
1. **Header bar**: Domain label, style name, pass number, library badges
2. **Main showcase area**: The visualization, animation, or graphic (fills 80%+ of viewport)
3. **Controls panel**: Domain-appropriate controls (filters for data-vis, play/pause for animation, regenerate for graphics)
4. **Info footer**: Technical details — library used, render method (Canvas/SVG/WebGL), data source

## Library Usage
- Load libraries from CDN `<script>` or `<link>` tags in `index.html`
- Initialize and use them in `app.js`
- Document usage in `README.md`
- The orchestrator's `libraryDirective` specifies which library to use — respect it

## Quality Standards
1. **No blank canvas**: The page must render visible content immediately on load
2. **Responsive**: Must look good on both desktop (1536px) and mobile (390px)
3. **Performance**: No freezing, no memory leaks, smooth animations
4. **Polish**: Professional presentation — consistent spacing, proper typography, clean layout
5. **Working interactivity**: All controls, tooltips, and interactive elements must function

## Playwright Screenshot Capture (MANDATORY FINAL STEP)

After generating all HTML/CSS/JS files, you MUST run the Playwright screenshot script before reporting the pass as complete.

**Command:**
```bash
node .claude/skills/visual-creative-subagent/scripts/validate-visuals-playwright.mjs --pass-dir <outputDir>
```

**What it produces:**
- `validation/desktop/showcase.png` — Screenshot at 1536x960
- `validation/mobile/showcase.png` — Screenshot at 390x844 @2x
- `validation/report.playwright.json` — Structured report

**Completion gate:** A pass is NOT complete until this script has run and produced screenshots + report.
